var controller = ((model, view)=>{

	// Everything inside here has to do with controlling the Data(from the Model) and the View

	// local vars
	var controllerVar = "full Controll";

	// functions






	// open vars and funcs
	return { 
		ctrlVar: controllerVar,
		modelly: model,
		viewy: view
	};
})(modelStuff, viewStuff);

console.log(controller.ctrlVar);
console.log(controller);

